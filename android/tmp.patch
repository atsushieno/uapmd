diff --git a/CMakeLists.txt b/CMakeLists.txt
index 92bbceb..0e02343 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -64,20 +64,16 @@ CPMAddPackage(
         DOWNLOAD_ONLY YES
 )
 
-cmake_path(SET CPPLOCATE_PATCH_FILE NORMALIZE "${CMAKE_CURRENT_LIST_DIR}/external/cpplocate-remove-pthread.patch")
-FetchContent_Declare(cpplocate
+CPMAddPackage(cpplocate
         NAME           cpplocate
         GIT_TAG        b796990d8df747758dbd6217b6315b7bdbb0c27e
         GIT_REPOSITORY https://github.com/cginternals/cpplocate
-        # required to mix CPP and Swift in the same project...
-        #PATCH_COMMAND        git apply "${CPPLOCATE_PATCH_FILE}"
-        #UPDATE_DISCONNECTED 1 # https://stackoverflow.com/questions/73718516/
         EXCLUDE_FROM_ALL
 )
+set (cpplocate_LIBRARIES cpplocate::cpplocate)
 # Force static build for cpplocate
 set(_UAPMD_OLD_BUILD_SHARED_LIBS_CPPL "${BUILD_SHARED_LIBS}")
 set(BUILD_SHARED_LIBS OFF)
-FetchContent_MakeAvailable(cpplocate)
 # Restore previous BUILD_SHARED_LIBS setting
 if(DEFINED _UAPMD_OLD_BUILD_SHARED_LIBS_CPPL AND NOT "${_UAPMD_OLD_BUILD_SHARED_LIBS_CPPL}" STREQUAL "")
     set(BUILD_SHARED_LIBS "${_UAPMD_OLD_BUILD_SHARED_LIBS_CPPL}")
diff --git a/src/remidy-tooling/CMakeLists.txt b/src/remidy-tooling/CMakeLists.txt
index cfc1aef..2633716 100644
--- a/src/remidy-tooling/CMakeLists.txt
+++ b/src/remidy-tooling/CMakeLists.txt
@@ -13,6 +13,8 @@ target_include_directories(remidy-tooling PRIVATE
         ../../include
         ${choc_SOURCE_DIR}
         ${cpplocate_SOURCE_DIR}/source/cpplocate/include
+        ${cpplocate_BINARY_DIR}/source/cpplocate/include
+        ${cpplocate_BINARY_DIR}/source/liblocate/include
         .
 )
 
@@ -26,7 +28,7 @@ endif ()
 
 target_link_libraries(remidy-tooling PRIVATE
         remidy::remidy
-        cpplocate::cpplocate
+        ${cpplocate_LIBRARIES}
         cpptrace::cpptrace
         ${remidy-tooling_LIBS_PLAT}
 )
diff --git a/src/remidy-tooling/PluginScanTool.cpp b/src/remidy-tooling/PluginScanTool.cpp
index 023e85f..5014dde 100644
--- a/src/remidy-tooling/PluginScanTool.cpp
+++ b/src/remidy-tooling/PluginScanTool.cpp
@@ -1,11 +1,15 @@
 
+#include <cpplocate/cpplocate.h>
 #include "remidy-tooling/PluginScanTool.hpp"
-#include "cpplocate/cpplocate.h"
 
 const char* TOOLING_DIR_NAME= "remidy-tooling";
 
 remidy_tooling::PluginScanTool::PluginScanTool() {
+#if ANDROID
+    std::filesystem::path dir{};
+#else
     auto dir = cpplocate::localDir(TOOLING_DIR_NAME);
+#endif
     plugin_list_cache_file = dir.empty() ? std::filesystem::path{""} : std::filesystem::path{dir}.append(
             "plugin-list-cache.json");
 
diff --git a/src/remidy/CMakeLists.txt b/src/remidy/CMakeLists.txt
index 6f16bdc..5fd747e 100644
--- a/src/remidy/CMakeLists.txt
+++ b/src/remidy/CMakeLists.txt
@@ -56,6 +56,7 @@ FetchContent_Declare(clap-helpers
 )
 FetchContent_MakeAvailable(clap-helpers)
 
+if (NOT ANDROID)
 # it requires gtkmm-3.0 but we don't need it
 set(SMTG_ENABLE_VSTGUI_SUPPORT OFF)
 set(SMTG_ENABLE_VST3_PLUGIN_EXAMPLES OFF)
@@ -71,18 +72,38 @@ FetchContent_Declare(vst3sdk
         EXCLUDE_FROM_ALL
 )
 FetchContent_MakeAvailable(vst3sdk)
-set(CMAKE_SKIP_INSTALL_RULES OFF)
 
 set(VST3_PUBLIC_SDK_MODULE_INFO_DIR ${vst3sdk_SOURCE_DIR}/public.sdk/source/vst/moduleinfo)
 
-add_library(remidy STATIC)
+set(VST3_SUPPORT_SOURCES
+        ${VST3_PUBLIC_SDK_MODULE_INFO_DIR}/moduleinfoparser.cpp
+        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/funknown.cpp
+        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/coreiids.cpp
+        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/ustring.cpp
+        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/conststringtable.cpp
+        ${vst3sdk_SOURCE_DIR}/public.sdk/source/vst/vstinitiids.cpp
+        ${vst3sdk_SOURCE_DIR}/public.sdk/source/common/commoniids.cpp
 
-target_compile_options(remidy PUBLIC
-        ${GTK3_CFLAGS}
-)
-target_link_options(remidy PUBLIC
-        ${GTK3_LDFLAGS}
+        vst3/VST3Helper.cpp
+        vst3/HostClasses.cpp
+        vst3/ClassModuleInfo.cpp
+        vst3/LinuxRunLoopIIDs.cpp
+        vst3/PluginFormatVST3.cpp
+        vst3/PluginInstanceVST3.cpp
+        vst3/PluginInstanceVST3.Buses.cpp
+        vst3/PluginInstanceVST3.Events.cpp
+        vst3/PluginInstanceVST3.Parameters.cpp
+        vst3/PluginInstanceVST3.Presets.cpp
+        vst3/PluginInstanceVST3.States.cpp
+        vst3/PluginInstanceVST3.UI.cpp
 )
+
+endif (NOT ANDROID)
+
+set(CMAKE_SKIP_INSTALL_RULES OFF)
+
+add_library(remidy STATIC)
+
 target_link_libraries(remidy PUBLIC
         ${X11_LIBRARIES}
         ${WAYLAND_CLIENT_LIBRARIES}
@@ -129,7 +150,7 @@ endif()
 
 target_link_libraries(remidy PRIVATE
         rtlog::rtlog
-        cpplocate::cpplocate
+        ${cpplocate_LIBRARIES}
         ${lv2kit_LIBRARY}
         midicci_static
         sdk # pretty bad naming from vst3sdk (shrug)
@@ -173,13 +194,7 @@ target_sources(remidy PRIVATE
         utils.cpp
         EventLoop.cpp
 
-        ${VST3_PUBLIC_SDK_MODULE_INFO_DIR}/moduleinfoparser.cpp
-        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/funknown.cpp
-        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/coreiids.cpp
-        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/ustring.cpp
-        ${vst3sdk_SOURCE_DIR}/pluginterfaces/base/conststringtable.cpp
-        ${vst3sdk_SOURCE_DIR}/public.sdk/source/vst/vstinitiids.cpp
-        ${vst3sdk_SOURCE_DIR}/public.sdk/source/common/commoniids.cpp
+        ${VST3_SUPPORT_SOURCES}
 
         Logger.cpp
         AudioBufferList.cpp
@@ -189,19 +204,6 @@ target_sources(remidy PRIVATE
         PluginBundlePool.cpp
         PluginFormat.cpp
 
-        vst3/VST3Helper.cpp
-        vst3/HostClasses.cpp
-        vst3/ClassModuleInfo.cpp
-        vst3/LinuxRunLoopIIDs.cpp
-        vst3/PluginFormatVST3.cpp
-        vst3/PluginInstanceVST3.cpp
-        vst3/PluginInstanceVST3.Buses.cpp
-        vst3/PluginInstanceVST3.Events.cpp
-        vst3/PluginInstanceVST3.Parameters.cpp
-        vst3/PluginInstanceVST3.Presets.cpp
-        vst3/PluginInstanceVST3.States.cpp
-        vst3/PluginInstanceVST3.UI.cpp
-
         au/AUv2Helper.cpp
         ${remidy_OBJC_SOURCES}
 
diff --git a/src/remidy/EventLoop.cpp b/src/remidy/EventLoop.cpp
index 61c0a59..c4be1a0 100644
--- a/src/remidy/EventLoop.cpp
+++ b/src/remidy/EventLoop.cpp
@@ -1,11 +1,25 @@
 #include "remidy.hpp"
+#if !ANDROID
 #include <choc/gui/choc_MessageLoop.h>
+#endif
 
-#if defined(__linux__) || defined(__unix__)
+#if defined(__linux__) || defined(__unix__) && !ANDROID && !EMSCRIPTEN
 #include "EventLoopLinux.hpp"
 #endif
 
 namespace remidy {
+#if defined(__linux__) || defined(__unix__)
+    // On Linux, use unified EventLoop with X11 + Wayland support (no GTK dependency)
+    EventLoopLinux linuxEventLoop{};
+
+    EventLoop* eventLoop{getEventLoop()};
+    EventLoop* getEventLoop() {
+        if (!eventLoop)
+            eventLoop = &linuxEventLoop;
+        return eventLoop;
+    }
+#else
+    // On macOS/Windows, use choc by default
     class EventLoopChoc : public EventLoop {
     protected:
         void initializeOnUIThreadImpl() override {
@@ -28,18 +42,6 @@ namespace remidy {
 
     EventLoopChoc choc{};
 
-#if defined(__linux__) || defined(__unix__)
-    // On Linux, use unified EventLoop with X11 + Wayland support (no GTK dependency)
-    EventLoopLinux linuxEventLoop{};
-
-    EventLoop* eventLoop{getEventLoop()};
-    EventLoop* getEventLoop() {
-        if (!eventLoop)
-            eventLoop = &linuxEventLoop;
-        return eventLoop;
-    }
-#else
-    // On macOS/Windows, use choc by default
     EventLoop* eventLoop{getEventLoop()};
     EventLoop* getEventLoop() {
         if (!eventLoop)
diff --git a/tools/uapmd-app/CMakeLists.txt b/tools/uapmd-app/CMakeLists.txt
index fe6f17f..731d863 100644
--- a/tools/uapmd-app/CMakeLists.txt
+++ b/tools/uapmd-app/CMakeLists.txt
@@ -212,7 +212,9 @@ foreach(app_target IN LISTS UAPMD_APP_TARGETS)
             ${midicci_SOURCE_DIR}/include
             ../../include
             ${libremidi_SOURCE_DIR}/include
-            ${cpplocate_SOURCE_DIR}/include
+            ${cpplocate_SOURCE_DIR}/source/cpplocate/include
+            ${cpplocate_BINARY_DIR}/source/cpplocate/include
+            ${cpplocate_BINARY_DIR}/source/liblocate/include
             ../remidy-imgui-shared
     )
 endforeach()
@@ -241,7 +243,7 @@ foreach(app_target IN LISTS UAPMD_APP_TARGETS)
             uapmd-engine
             remidy-gui
             remidy-imgui-shared
-            cpplocate::cpplocate
+            ${cpplocate_LIBRARIES}
             libremidi::libremidi
             midicci_static
             zlib-ng
diff --git a/tools/uapmd-app/gui/ScriptEditor.cpp b/tools/uapmd-app/gui/ScriptEditor.cpp
index abe1a4e..4d894c4 100644
--- a/tools/uapmd-app/gui/ScriptEditor.cpp
+++ b/tools/uapmd-app/gui/ScriptEditor.cpp
@@ -1,6 +1,8 @@
 #include "ScriptEditor.hpp"
 #include <imgui.h>
+#if !ANDROID
 #include <cpplocate/cpplocate.h>
+#endif
 #include <filesystem>
 #include <fstream>
 #include <sstream>
@@ -285,12 +287,16 @@ std::string ScriptEditor::getJsLibraryPath (const std::string& modulePath) const
     else if (cleanPath.starts_with ("/"))
         cleanPath = cleanPath.substr (1);
 
+#if ANDROID
+    std::filesystem::path exeDir{};
+#else
     // Use cpplocate to find the executable directory
     std::filesystem::path exeDir = cpplocate::getExecutablePath();
     if (! exeDir.empty())
         exeDir = exeDir.parent_path();
     else
         exeDir = std::filesystem::current_path();  // Fallback
+#endif
 
     // Try different base paths for module resolution
     std::vector<std::filesystem::path> basePaths = {
