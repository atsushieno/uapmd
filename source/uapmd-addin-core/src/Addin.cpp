#include <uapmd-addin-core/uapmd-addin-core.hpp>

// The v1 addin API is intentionally interface-only.  This translation unit
// makes the module an ordinary UAPMD library and reserves a home for shared
// implementation that does not belong in an application loader.
